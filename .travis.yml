language: node_js

node_js:
  - 10.15.3

cache: yarn

jobs:
  include:
    - stage: release
      node_js: 10.15.3
      deploy:
        provider: script
        on:
          all_branches: true
        skip_cleanup: true
        script:
          - npx semantic-release
